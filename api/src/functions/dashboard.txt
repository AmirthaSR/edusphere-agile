import { app } from "@azure/functions";

app.http("dashboard", {
    methods: ["GET"],
    authLevel: "anonymous",
    handler: async (request, context) => {

        return {
            status: 200,
            jsonBody: {
                message: "Welcome to EduSphere Dashboard 🚀",
                data: {
                    courses: ["Math", "AI", "Cloud"],
                    progress: "70%"
                }
            }
        };
    }
});